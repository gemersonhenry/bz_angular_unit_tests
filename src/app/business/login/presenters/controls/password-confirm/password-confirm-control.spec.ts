import { PasswordConfirmControl } from './password-confirm-control';

describe('PasswordConfirmControl', () => {
  it('should create an instance', () => {
    expect(new PasswordConfirmControl()).toBeTruthy();
  });
});
